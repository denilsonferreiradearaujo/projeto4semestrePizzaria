/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: false, // Desabilita a minificação para evitar problemas no hot reload
};
export default nextConfig;
